'use strict';

describe('myApp.tobeDone module', function() {

  beforeEach(module('myApp.tobeDone'));

  describe('tobeDone controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('tobeDoneCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});