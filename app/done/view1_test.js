'use strict';

describe('myApp.done module', function() {

  beforeEach(module('myApp.done'));

  describe('done controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('doneCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});